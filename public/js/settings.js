"use strict";
const token = () => localStorage.getItem("vsbil_access_token");
const headers = () => ({ Authorization: `Bearer ${token()}`, Accept: "application/json" });
const toast = document.getElementById("toast");
function show(message){toast.textContent=message;toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),3500)}
async function api(url, options={}){const response=await fetch(url,{...options,headers:{...headers(),...(options.headers||{})}});const data=await response.json().catch(()=>null);if(response.status===401){location.href="/login.html";throw new Error("Authentication required")}if(!response.ok)throw new Error(data?.message||"Request failed");return data}
document.querySelectorAll(".settings-nav button").forEach(button=>button.addEventListener("click",()=>{document.querySelectorAll(".settings-nav button").forEach(x=>x.classList.remove("active"));document.querySelectorAll(".settings-section").forEach(x=>x.classList.remove("active"));button.classList.add("active");document.getElementById(button.dataset.section).classList.add("active")}));
async function loadAccount(){try{const data=await api("/api/auth/me");const user=data.user;document.getElementById("username").textContent=user.username||"VSBIL user";document.getElementById("email").textContent=user.email||"";const status=document.getElementById("accountStatus");status.textContent=user.status||"Unknown";status.classList.toggle("ok",user.status==="active");}catch(error){show(error.message)}}
async function loadYoutube(){try{const data=await api("/api/youtube/connection");const state=document.getElementById("youtubeState");const connect=document.getElementById("connectYoutube");const disconnect=document.getElementById("disconnectYoutube");if(data.connected){const c=data.connection;state.innerHTML=`<div class="channel"><img src="${c.thumbnail_url||""}" alt=""><div><strong>${escapeHtml(c.channel_title)}</strong><div class="muted">${escapeHtml(c.channel_url)}</div><span class="status ok">Connected</span></div></div>`;connect.hidden=true;disconnect.hidden=false}else{state.innerHTML='<p class="muted">No YouTube channel is connected.</p>';connect.hidden=false;disconnect.hidden=true}}catch(error){show(error.message)}}
function escapeHtml(value){return String(value??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;")}
document.getElementById("connectYoutube").addEventListener("click",()=>{location.href="/api/youtube/connect"});
document.getElementById("disconnectYoutube").addEventListener("click",async()=>{if(!confirm("Disconnect your YouTube account? You may lose access to YouTube verification features."))return;try{await api("/api/youtube/connection",{method:"DELETE"});show("YouTube disconnected");loadYoutube()}catch(error){show(error.message)}});
document.getElementById("sendCode").addEventListener("click",async()=>{try{await api("/api/verification/send",{method:"POST"});document.getElementById("codeBox").hidden=false;show("Verification code sent to your email")}catch(error){show(error.message)}});
document.getElementById("verifyCode").addEventListener("click",async()=>{const code=document.getElementById("code").value.trim();try{await api("/api/verification/confirm",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({code})});document.getElementById("emailStatus").textContent="Verified";document.getElementById("emailStatus").classList.add("ok");document.getElementById("codeBox").hidden=true;show("Email verified successfully")}catch(error){show(error.message)}});
loadAccount();loadYoutube();
