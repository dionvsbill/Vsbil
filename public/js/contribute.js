"use strict";
const p=new URLSearchParams(location.search),slug=p.get('campaign'),$=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
async function load(){if(!slug)return;const r=await fetch('/api/business/funerals/public/'+encodeURIComponent(slug));const d=await r.json();if(!r.ok)throw Error(d.message||'Campaign unavailable');const c=d.campaign;$('#campaign h1').textContent=c.title;$('#story').textContent=c.story||'';const raised=Number(c.amount_raised||0),target=Number(c.target_amount||0);$('#raised').textContent=`GH₵${raised.toFixed(2)}`;$('#target').textContent=target?`Target GH₵${target.toFixed(2)}`:'No target';$('#progressBar').style.width=target?`${Math.min(100,raised/target*100)}%`:'0%'}
$('#contributionForm').addEventListener('submit',async e=>{e.preventDefault();const o=Object.fromEntries(new FormData(e.target).entries());o.anonymous=Boolean(o.anonymous);try{const r=await fetch('/api/business/funerals/public/'+encodeURIComponent(slug)+'/contributions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(o)});const d=await r.json();if(!r.ok)throw Error(d.message||'Unable to create contribution');$('#message').textContent='Contribution created. Connect this reference to the payment provider checkout before treating it as paid: '+d.reference;e.target.reset()}catch(x){$('#message').textContent=x.message}});load().catch(e=>$('#campaign h1').textContent=e.message);